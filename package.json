{
  "name": "horarium",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first, browser-only course scheduler for NYU Abu Dhabi",
  "type": "module",
  "scripts": {
    "planner": "npx serve planner -p 3000",
    "test": "node --test planner/src/**/*.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
